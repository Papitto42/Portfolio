# Site files live in portfolio/ — publish that folder as the site root
[build]
  publish = "portfolio"
  command = "echo 'No build step required'"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
